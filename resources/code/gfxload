//Texture loader (NOT USED)
if directory_exists(working_directory + '\resources\textures\')
{
    execute_file(working_directory+'\resources\textures\gfx',0);
} else {
    show_error("Directory does not exist!", false);
}