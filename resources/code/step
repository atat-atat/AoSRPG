//Solid collisions
h=argument0;
v=argument1;

for(i=abs(h); i>0; i-=1){
    if(place_free(x+(h/abs(h)),y)) x+=h/abs(h)
    else break;
};

for(i=abs(v); i>0; i-=1){
    if(place_free(x,y+(v/abs(v)))) y+=v/abs(v)
    else break;
};